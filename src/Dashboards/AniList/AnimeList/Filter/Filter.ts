import HTML from "../../../../components/HTML/HTML";

export default function Filter() {
  const styles = "w-60 h-96 border-2 border-rose-800 rounded-lg";
  const filterContainer = HTML("div", styles);
  return filterContainer;
}
