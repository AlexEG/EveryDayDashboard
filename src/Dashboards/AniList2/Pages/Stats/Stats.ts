import HTML from "../../../../components/HTML/HTML";

export default function Stats() {
  const styles = "w-full h-96 border";

  const stats = HTML("section", styles, "anilist--stats");

  return stats;
}
